<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - JapaneseMealReservation</title>

    <link href="https://cdn.jsdelivr.net/npm/fullcalendar@6.1.17/main.min.css" rel="stylesheet" />
    <link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.min.css" />
    <link rel="stylesheet" href="~/css/site.css" asp-append-version="true" />
    <link rel="stylesheet" href="~/JapaneseMealReservation.styles.css" asp-append-version="true" />
    <link href="~/css/style.css" rel="stylesheet" asp-append-version="true" />

</head>

<body>

    @if (ViewData["ShowDashboardHeader"] as bool? == true)
    {
        <header class="fixed-top">
            <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
                <div class="container-fluid px-3">
                    <!-- Logo -->
                    <a class="navbar-brand d-flex align-items-center" asp-controller="Home" asp-action="Index">
                        <img src="~/images/JMRS-logo.png" class="jrms-logo me-2" alt="Japanese Meal Reservation System" style="height: 40px;" />
                    </a>

                    <!-- Mobile Toggle Button -->
                    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarResponsive"
                    aria-controls="navbarResponsive" aria-expanded="false" aria-label="Toggle navigation">
                        <span class="navbar-toggler-icon"></span>
                    </button>

                    <!-- Navbar Links -->
                    <div class="collapse navbar-collapse" id="navbarResponsive">
                        <!-- Greeting -->
                        @if (User?.Identity?.IsAuthenticated == true)
                        {
                            var firstName = User.FindFirst(System.Security.Claims.ClaimTypes.GivenName)?.Value;
                            var lastName = User.FindFirst(System.Security.Claims.ClaimTypes.Surname)?.Value;

                            <span class="navbar-text text-white small text-nowrap">
                                👋 Welcome, <strong>@firstName @lastName</strong>
                            </span>
                        }

                        <div class="d-lg-flex align-items-center gap-3 ms-auto">
                            <!-- Translate (optional) -->
                            <div id="google_translate_element" class="text-light me-3 small d-none d-lg-block">
                                Translate:
                            </div>

                            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                                <li class="nav-item">
                                    <a class="nav-link link-warning text-light" asp-controller="Home" asp-action="Index">
                                        <i class="fa-solid fa-house"></i> Home
                                    </a>
                                </li>

                                <li class="nav-item">
                                    <a class="nav-link link-warning text-light" asp-controller="Reservation" asp-action="MealReservation">
                                        <i class="fa-solid fa-calendar-week"></i> Menu
                                    </a>
                                </li>

                                <li class="nav-item">
                                    <a class="nav-link  link-warning text-light" asp-controller="Order" asp-action="OrderSummary">
                                        <i class="fa-solid fa-cart-shopping"></i> My Orders
                                    </a>
                                </li>

                                @if (User.HasClaim("EmployeeType", "Expat"))
                                {
                                    <li class="nav-item">
                                        <a class="nav-link link-warning text-light" asp-controller="Reservation" asp-action="ExpatAdvanceReservation">
                                            <i class="fa-solid fa-border-all"></i> Expat Reservation
                                        </a>
                                    </li>
                                }

                            </ul>

                            <!-- Book Now Button -->
                            <a class="btn btn-warning btn-sm text-dark" asp-controller="Reservation" asp-action="AdvanceOrdering" style="border-radius: 20px;">
                                Book Now
                            </a>

                            <!-- Admin Dashboard -->
                            @if (User.Identity.IsAuthenticated && User.IsInRole("ADMIN"))
                            {
                                <a class="btn btn-primary btn-sm text-white" asp-controller="Admin" asp-action="Dashboard" style="border-radius: 20px;">
                                    Admin Dashboard
                                </a>
                            }

                            <!-- Logout Button -->
                            @if (User.Identity?.IsAuthenticated == true)
                            {
                                @* <form method="post" asp-controller="Home" asp-action="Logout" class="d-inline">
                                    <button type="submit" class="btn btn-danger btn-sm" style="border-radius: 20px;">Logout</button>
                                </form> *@
                                <!-- User Admin Dropdown -->
                                <div class="dropdown d-inline-block">
                                    <button class="btn btn-light dropdown-toggle d-flex align-items-center"
                                            type="button" id="userAdminDropdown"
                                            data-bs-toggle="dropdown" aria-expanded="false" style="border-radius: 50px;">
                                        <i class="fa-solid fa-user-gear me-2"></i>
                                    </button>

                                    <ul class="dropdown-menu dropdown-menu-end shadow" aria-labelledby="userAdminDropdown">
                                        <!-- Add User -->
                                        @if (User.Identity.IsAuthenticated && User.IsInRole("ADMIN"))
                                        {
                                            <li class="px-3">
                                                <button type="button" class="btn btn-primary btn-sm w-100" data-bs-toggle="modal" data-bs-target="#userAdminModal">
                                                    <i class="fa-solid fa-user-plus me-2"></i> Add Admin
                                                </button>
                                            </li>

                                            <!-- Divider -->
                                            <li><hr class="dropdown-divider" /></li>
                                        }

                                       

                                        <!-- Logout -->
                                        <li>
                                            <form method="post" asp-controller="Home" asp-action="Logout" class="px-3">
                                                <button type="submit" class="btn btn-danger btn-sm w-100" style="">
                                                    <i class="fa-solid fa-right-from-bracket me-1"></i> Logout
                                                </button>
                                            </form>
                                        </li>
                                    </ul>
                                </div>

                            }
                        </div>
                    </div>
                </div>
            </nav>
        </header>
    }



    <div class="">
        <main role="main" class="">
            @RenderBody()
        </main>
    </div>

 
    <!-- User Admin Modal -->
    <div class="modal fade" id="userAdminModal" tabindex="-1" aria-labelledby="userAdminModalLabel" aria-hidden="true">
        <div class="modal-dialog modal-lg">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title" id="userAdminModalLabel">Update User to Admin</h5>
                    <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                </div>

                <div class="modal-body">
                    <!-- Search Bar -->
                    <div class="input-group mb-3">
                        <input type="text" id="searchUser" class="form-control" placeholder="Search user by name">
                        <button class="btn btn-outline-secondary" type="button" id="btnSearch">Search</button>
                    </div>

                    <!-- Table -->
                    <table class="table table-hover table-bordered">
                        <thead class="table-light">
                            <tr>
                                <th>ADID</th>
                                <th>First Name</th>
                                <th>Last Name</th>
                                <th>Email</th>
                                <th>Role</th>
                                <th>Action</th>
                            </tr>
                        </thead>
                        <tbody id="userTableBody">
                            <!-- Rows will be inserted here dynamically -->
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>

   

    @section Scripts {
        <partial name="_ValidationScriptsPartial" />
    }


    @if (ViewData["ShowFooter"] as bool? == true)
    {
        <footer class="footer-fix d-flex justify-content-between align-items-center">
            <span class="text-muted"> &copy; 2025 - BIPH - Japanese Meal Reservation | Developed by: BPS Application Developer</span>
            <a href="#"
            class="manual-btn rounded-pill px-3 py-1 shadow-sm d-inline-flex align-items-center gap-2 border border-warning text-warning text-decoration-none">
                <i class="fa-solid fa-book"></i> Manual
            </a>
        </footer>
    }

    <!-- ⚙️ Google Translate Setup Script -->
    <script>
        function googleTranslateElementInit() {
            new google.translate.TranslateElement({
              pageLanguage: 'en',
              includedLanguages: 'en,ja',
              layout: google.translate.TranslateElement.InlineLayout.HORIZONTAL // or SIMPLE, VERTICAL
            }, 'google_translate_element');
        }
    </script>

    <script src="//translate.google.com/translate_a/element.js?cb=googleTranslateElementInit"></script>
    <!-- ⚙️ Google Translate Setup Script End -->

    <script src="~/lib/jquery/dist/jquery.min.js"></script>
    <script src="~/lib/bootstrap/dist/js/bootstrap.bundle.min.js"></script>
    <script src="~/js/site.js" asp-append-version="true"></script>

    <script src='https://cdn.jsdelivr.net/npm/fullcalendar@6.1.17/index.global.min.js'></script>
    @* Fontawesome *@
    <script src="https://use.fontawesome.com/releases/v6.3.0/js/all.js" crossorigin="anonymous"></script>

    <!-- isotope js -->
    <script src="https://unpkg.com/isotope-layout@3/dist/isotope.pkgd.min.js"></script>

    <!-- Sweet alert -->
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>

    <!-- PrintThis -->
    <script src="https://cdnjs.cloudflare.com/ajax/libs/printThis/1.15.0/printThis.min.js"></script>
    <script src="~/js/printTable.js"></script>

    <!-- xlsx.full -->
    <script src="https://cdnjs.cloudflare.com/ajax/libs/xlsx/0.18.5/xlsx.full.min.js"></script>

    <script src="~/js/custom.js"></script>
   
    @if (ViewData["Title"] as string == "Reservation")  
    {
        <script src="~/js/updateMenuModalHandler.js"></script>
        <script src="~/js/orderValidation.js"></script>
    }

    @if (ViewData["Title"] as string == "Dashboard")
    {
        <script src="~/js/updateDashboardModal.js"></script>
    }

    @if (ViewData["Title"] as string == "Registration")
    {
        <script>
                  window.appBaseUrl = '@Url.Content("~/")';
        </script>
      
        <script src="~/js/registration.js"></script>
    }

    @if (ViewData["Title"] as string == "Advance Reservation")
    {
        <script src="~/js/reservation.js"></script>
        <script src="~/js/full-calendar.js"></script>
    }

    @if (ViewData["Title"] as string == "Order Details")
    {
        <script src="~/js/orderCalendar.js"></script>
    }

    @* For Upadating of User Role -> Admin *@
    <!-- SweetAlert2 -->
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>

    <script>
        document.getElementById("btnSearch").addEventListener("click", function () {
            const query = document.getElementById("searchUser").value.trim();

            // ✅ Validation for empty search
            if (!query) {
                Swal.fire({
                    icon: 'warning',
                    title: 'Empty Search',
                    text: 'Please enter a name or email to search.',
                    confirmButtonColor: '#3085d6'
                });
                return;
            }

            fetch('@Url.Action("Search", "Admin")?query=' + encodeURIComponent(query))
                .then(res => res.json())
                .then(data => {
                    const tbody = document.getElementById("userTableBody");
                    tbody.innerHTML = "";

                    if (data.length === 0) {
                        Swal.fire({
                            icon: 'info',
                            title: 'No Results',
                            text: 'No users found matching your search.',
                            confirmButtonColor: '#3085d6'
                        });
                        return;
                    }

                    data.forEach(user => {
                        tbody.innerHTML += `
                            <tr>
                                <td>${user.adid}</td>
                                <td>${user.firstName}</td>
                                <td>${user.lastName}</td>
                                <td>${user.email}</td>
                                <td>
                                    <select id="roleSelect_${user.id}" class="form-select form-select-sm">
                                        <option value="EMPLOYEE" ${user.role === "EMPLOYEE" ? "selected" : ""}>EMPLOYEE</option>
                                        <option value="ADMIN" ${user.role === "ADMIN" ? "selected" : ""}>ADMIN</option>
                                    </select>
                                </td>
                                <td>
                                    <button class="btn btn-sm btn-success" onclick="updateRole(${user.id})">Update</button>
                                </td>
                            </tr>`;
                    });
                })
                .catch(() => {
                    Swal.fire({
                        icon: 'error',
                        title: 'Server Error',
                        text: 'Something went wrong while fetching users.',
                        confirmButtonColor: '#d33'
                    });
                });
        });

         function updateRole(userId) {
            const selectedRole = document.getElementById(`roleSelect_${userId}`).value;

            Swal.fire({
                title: 'Are you sure?',
                text: `This user will be updated to ${selectedRole}.`,
                icon: 'warning',
                showCancelButton: true,
                confirmButtonColor: '#28a745',
                cancelButtonColor: '#d33',
                confirmButtonText: `Yes, make ${selectedRole}`
            }).then((result) => {
                if (!result.isConfirmed) return;

                fetch('@Url.Action("UpdateRole", "Admin")/' + userId, {
                    method: "POST",
                    headers: { "Content-Type": "application/json" },
                    body: JSON.stringify({ role: selectedRole })
                })
                .then(res => {
                    if (!res.ok) throw new Error("Failed to update");
                    return res.json();
                })
                .then(() => {
                    Swal.fire({
                        icon: 'success',
                        title: 'Success',
                        text: `User updated to ${selectedRole} successfully!`,
                        confirmButtonColor: '#28a745'
                    }).then(() => {
                        // Clear search box and results
                        document.getElementById("searchUser").value = "";
                        document.getElementById("userTableBody").innerHTML = "";
                    });
                })
                .catch(() => {
                    Swal.fire({
                        icon: 'error',
                        title: 'Error',
                        text: 'Something went wrong while updating user.',
                        confirmButtonColor: '#d33'
                    });
                });
            });
        }


    </script>
    
    @await RenderSectionAsync("Scripts", required: false)

</body>
</html>
